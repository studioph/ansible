- name: Install docker
  ansible.builtin.import_role:
    name: apt-external
  vars:
    item: "{{ docker }}"

- name: Add user to docker group
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    groups: docker
    append: true
  become: true